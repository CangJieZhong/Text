<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper SYSTEM "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<!--namespace: 命名空间, 每一个sql映射文件的namespace要唯一 在以后,这个namespace特殊写法 -->
<!-- 这里要改 -->
<mapper namespace="dao.UserMapper">
	<select id="queryUserInfo" resultType="Logininfo">
		SELECT * FROM logininfo u WHERE 
		u.username=#{username} 
		AND u.password = #{password}
		AND usertype = #{usertype}
	</select>
	<insert id="addUserinfo">
		INSERT	INTO logininfo(username,PASSWORD,state,usertype) VALUES(#{username},#{password},0,0)
	</insert>
	<select id="queryUsernameInfo" resultType="string">
		SELECT u.username FROM logininfo u WHERE u.username=#{username} and usertype = #{usertype} 
	</select>
</mapper>
